import { Dimensions } from 'react-native';

const config = {
    deviceWidth: Dimensions.get('window').width,
    deviceHeight: Dimensions.get('window').height,
    url: "https://bonbangsasu.com"
}

export default config